// import { useLoaderData } from "react-router-dom";

const ToyDetails = () => {
    // const loadedToy = useLoaderData();
    // console.log(loadedToy);
    // const { _id, toy_picture, toy_name, price, rating } = loadedToy;
    return (
        <div>
            <h2 className='text-4xl text-center font-semibold mb-8'>Toy Details Page</h2>
            
        </div>
    );
};

export default ToyDetails;